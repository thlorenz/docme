/**
 * Actually this function that does it all, but better.
 *
 * @name someOtherBetterFunction
 * @function
 * @param {String} beep for peeps
 * @param {String} boop for poops 
 * @param {Number} times how many times to do it
 * @param {Function} strategy how to do it
 */
function someOtherBetterFunction (beep, boop, times, strategy) {
}
